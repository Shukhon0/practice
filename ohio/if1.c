#include <stdio.h>
void main () {
    int a,num;
    printf("Print a and num respectively.\n");
    scanf("%d %d",&a,&num);
    if (a!=0 && num/a){
        printf("sigma");
    }
    else printf("alpha");
} 